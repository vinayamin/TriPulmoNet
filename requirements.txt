# Versions recorded in the training/evaluation runtime (environment_and_config.json, variantC_lock_record.json,
# notebook cell 2 output). Colab, NVIDIA L4, Python 3.12.13. The +cu128 builds were used; CPU builds of the
# same versions are expected to be equivalent but were not tested.
torch==2.11.0
torchvision==0.26.0
timm==1.0.27
torchxrayvision==1.5.2
scikit-learn==1.6.1
numpy==2.0.2
pandas==2.2.2
opencv-python-headless==4.13.0.*
pillow==11.3.0
pydicom==3.0.2
scipy
